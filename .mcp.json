{
  "mcpServers": {
    "pipedrive": {
      "type": "http",
      "url": "${PIPEDRIVE_MCP_URL:-https://vercel-app-talentir.vercel.app/api/mcp}",
      "headers": {
        "Authorization": "Bearer ${PIPEDRIVE_API_TOKEN}"
      }
    }
  }
}
